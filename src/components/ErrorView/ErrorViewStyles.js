import { StyleSheet } from "react-native";

export default StyleSheet.create({
    errorTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 12,
    },
    errorLabel: {
        fontSize: 16,
        marginLeft: 25,
        marginBottom: 24,
    },
});