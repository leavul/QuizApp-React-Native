import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 15,
        borderRadius: 8,
    },
    buttonText: {
        fontSize: 14,
        textAlign: 'center',
        color: '#ffffffff',
        fontWeight: 'bold',

    },
});