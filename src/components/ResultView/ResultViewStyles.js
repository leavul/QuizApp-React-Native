import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    resultContainer: {
        alignItems: 'center',
    },
    resultTitle: {
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 20,
    },
    resultLabel: {
        fontSize: 18,
        marginBottom: 10,
    },
    resultScore: {
        fontSize: 20,
        fontWeight: '600',
        marginBottom: 20,
    },
});